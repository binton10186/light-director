<UserControl x:Class="LightDirector.Modules.Toolbar.Views.ToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LightDirector.Modules.Toolbar.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
      <ToolBarTray>
      <ToolBar>
         <Button Margin="5,0,0,0" Command="{Binding PlayCommand}">Play</Button>
         <Button Margin="5,0,0,0" Command="{Binding PauseCommand}">Pause</Button>
      </ToolBar>
         <ToolBar>
            <Slider Value="{Binding Volume}" Minimum="0" Maximum="100" Width="200"/>
         </ToolBar>
      </ToolBarTray>
   </Grid>
</UserControl>
